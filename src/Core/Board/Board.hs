module Core.Board.Board () where
import Core.Board.Tile
import Core.Board.Actor

{- generateBoard (a, b)
   PRE:       a and b > 0
   POST:      A random board of size a x b.
   EXAMPLES:  generateBoard ==
-}
generateBoard :: (Float, Float) -> a
generateBoard _ = undefined
